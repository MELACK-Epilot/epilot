# 📦 INSTALLATION DÉPENDANCES RADIX UI

**Date** : 7 novembre 2025

---

## ⚠️ DÉPENDANCES MANQUANTES

Les composants UI nécessitent plusieurs packages Radix UI.

---

## 🚀 INSTALLATION RAPIDE

**Commande unique** (recommandé) :
```bash
npm install @radix-ui/react-popover @radix-ui/react-dialog @radix-ui/react-select @radix-ui/react-label @radix-ui/react-separator @radix-ui/react-progress
```

---

## 📋 OU INSTALLATION PAR PACKAGE

Si vous préférez installer un par un :

### **1. Popover** (pour PaymentFilters)
```bash
npm install @radix-ui/react-popover
```

### **2. Dialog** (pour modals)
```bash
npm install @radix-ui/react-dialog
```

### **3. Select** (pour dropdowns)
```bash
npm install @radix-ui/react-select
```

### **4. Label** (pour formulaires)
```bash
npm install @radix-ui/react-label
```

### **5. Separator** (pour séparateurs)
```bash
npm install @radix-ui/react-separator
```

### **6. Progress** (pour barres progression)
```bash
npm install @radix-ui/react-progress
```

---

## ✅ VÉRIFICATION

Après installation, vérifiez dans `package.json` :

```json
{
  "dependencies": {
    "@radix-ui/react-popover": "^1.0.7",
    "@radix-ui/react-dialog": "^1.0.5",
    "@radix-ui/react-select": "^2.0.0",
    "@radix-ui/react-label": "^2.0.2",
    "@radix-ui/react-separator": "^1.0.3",
    "@radix-ui/react-progress": "^1.0.3",
    "react-day-picker": "^8.10.0"
  }
}
```

---

## 🔧 COMMANDE COMPLÈTE

**Copiez-collez cette commande** :

```bash
npm install @radix-ui/react-popover @radix-ui/react-dialog @radix-ui/react-select @radix-ui/react-label @radix-ui/react-separator @radix-ui/react-progress
```

Puis redémarrez :

```bash
npm run dev
```

---

## 📊 PACKAGES DÉJÀ INSTALLÉS

Ces packages devraient déjà être présents :
- ✅ react
- ✅ react-dom
- ✅ framer-motion
- ✅ lucide-react
- ✅ recharts
- ✅ react-day-picker (installé précédemment)

---

## 🎯 COMPOSANTS CONCERNÉS

Ces composants utilisent Radix UI :

**Popover** :
- PaymentFilters (calendrier)

**Dialog** :
- PaymentDetailsModal
- Expenses (création/détails)

**Select** :
- Tous les dropdowns

**Label** :
- Tous les formulaires

**Separator** :
- PaymentDetailsModal

**Progress** :
- BudgetManager

---

## ⏱️ TEMPS D'INSTALLATION

**~30 secondes** pour installer tous les packages

---

**APRÈS INSTALLATION, TOUT FONCTIONNERA !** ✅
